# Master Receptionists — Dashboard Specification v2
## Frontend Architecture & Backend Wiring Guide

---

## 1. Overview

This document defines the complete specification for the Master Receptionists customer dashboard. It covers authentication, page structure, data sources, editable parameters, and the relationship between the frontend and the existing Supabase backend.

**Tech stack:**
- Frontend: React (Vite) + Tailwind CSS + shadcn/ui
- Auth: Supabase Auth (email/password + Google OAuth)
- Database: Supabase (PostgreSQL) — all tables in `public` schema
- Hosting: Vercel (app.masterreceptionists.com)
- Charts: Recharts (React-native charting library)

**Build approach:**
- Phase 1 (Gemini): Build the complete visual scaffold with mock/hardcoded data. No Supabase client, no real queries, no auth wiring. Pure React components with realistic placeholder data that matches the shapes described in this spec.
- Phase 2 (Backend wiring): Replace mock data with real Supabase queries, wire up authentication, add RLS policies, and connect all editable parameters to the database.

**User model:**
- Each user account is linked to one organization via a `user_org_memberships` table
- Users see only data scoped to their `org_id`
- Future-proofed: the membership table supports multiple users per org with roles (owner, admin, viewer)

---

## 2. Authentication

### 2.1 Auth Flow (to be wired in Phase 2)
Supabase Auth handles all session management, token refresh, and password reset flows.

**Sign Up flow:**
1. User enters email + password (or clicks "Sign in with Google")
2. Supabase creates the auth user
3. A row is created in `user_org_memberships` with `role = 'owner'` and `org_id = NULL`
4. Admin manually sets the `org_id` to link them to their organization
5. Until org_id is set, the user sees an "Account Pending" placeholder screen

**Sign In flow:**
1. Email/password or Google OAuth → Supabase Auth → session token
2. App queries `user_org_memberships` to get `org_id`
3. All subsequent data queries are scoped to that `org_id`

**Google OAuth for login:**
- Uses Supabase's built-in `signInWithOAuth({provider: 'google'})` — same Google Cloud project, `email/profile` scope only
- This is separate from the calendar OAuth (which uses the `calendar` scope and stores refresh tokens)
- Same post-login flow: look up org membership, scope data accordingly

**Password reset:**
- Supabase Auth handles this via email link — built-in, no custom code needed

### 2.2 Database Table (already created)

```sql
-- Links Supabase Auth users to organizations
CREATE TABLE user_org_memberships (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id     UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    org_id      TEXT REFERENCES organization_config(org_id),  -- NULL until admin links it
    role        TEXT NOT NULL DEFAULT 'owner',  -- 'owner', 'admin', 'viewer'
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE(user_id, org_id)
);
```

### 2.3 Auth States (Gemini should build all three screens)
- **Not logged in** → Show login/signup page with email/password form and "Sign in with Google" button
- **Logged in, no org linked** → Show "Account Pending" page: "Your account is being set up. We'll notify you when your dashboard is ready. If you have questions, contact support@masterreceptionists.com"
- **Logged in, org linked** → Show full dashboard

---

## 3. Page Structure & Navigation

### Sidebar Navigation
```
📊  Dashboard        (home/overview — KPI cards and charts)
📞  Call History      (searchable call log table with expandable detail view)
📅  Calendars        (schedule editor, overrides, calendar settings, authorization)
⚙️  Settings         (block list, notification toggles, account)
```

### Layout
- Dark sidebar (left) with navigation items and org name at top
- Light content area (right) with page header and content
- Mobile responsive — sidebar collapses to hamburger menu on small screens
- Top-right corner: user avatar/email with dropdown (Account Settings, Sign Out)

---

## 4. Page: Dashboard (Home/Overview)

The landing page after login. Shows high-level summary and trends.

### 4.1 Time Period Selector
A dropdown or toggle at the top of the page: "This Month" / "Last 30 Days" / "Last 7 Days" / "This Year"
This controls which data is displayed in the cards and charts below.

### 4.2 KPI Summary Cards (top row — 4 cards)

| Card | Mock Value | Description | Data Source (Phase 2) |
|------|-----------|-------------|----------------------|
| Total Calls | 147 | Calls in selected period | `kpi_org_monthly_summary.total_calls` |
| Appointments Booked | 43 | Active bookings in period | `kpi_org_monthly_summary.total_bookings` |
| Total Cost | $4.52 | Platform cost in period | `kpi_org_monthly_summary.total_cost` |
| Avg Call Duration | 1m 24s | Average call length | `kpi_org_monthly_summary.avg_call_duration_seconds` |

### 4.3 Secondary KPI Cards (second row — 4 cards)

| Card | Mock Value | Description | Data Source (Phase 2) |
|------|-----------|-------------|----------------------|
| Unique Callers | 38 | Distinct phone numbers | `kpi_org_monthly_summary.unique_callers` |
| Booking Conversion | 29.3% | % of calls that result in a booking | `kpi_booking_conversion_rate.conversion_rate_pct` |
| Cancellation Rate | 4.7% | % of bookings cancelled | `kpi_cancellation_rate.cancellation_rate_pct` |
| Cost Per Booking | $0.11 | Average cost per booked appointment | `kpi_cost_per_booking.cost_per_booking` |

### 4.4 Charts Section

**Chart 1: Call Volume Over Time** (line or bar chart)
- X-axis: dates (daily for 30-day view, weekly for yearly view)
- Y-axis: number of calls
- Mock: 30 data points with realistic variation (5-15 calls per day)
- Data source: `kpi_call_minutes_by_org_daily` (total_calls by day)

**Chart 2: Bookings Over Time** (line or bar chart)
- X-axis: dates
- Y-axis: number of bookings
- Two series: "Bot Bookings" and "Manual Bookings" (stacked or grouped)
- Mock: 30 data points
- Data source: `kpi_bookings_by_calendar_daily` (bot_bookings, manual_bookings by day)

**Chart 3: Calls by Hour of Day** (bar chart)
- X-axis: hours (6 AM - 10 PM)
- Y-axis: total calls at that hour
- Mock: distribution peaking at 10 AM and 2 PM
- Data source: `kpi_calls_by_hour` (hour_of_day, total_calls)

**Chart 4: Calls by Day of Week** (bar chart)
- X-axis: Mon–Sun
- Y-axis: total calls
- Mock: higher on weekdays, lower on weekends
- Data source: `kpi_calls_by_day_of_week` (day_name, total_calls)

### 4.5 Additional Dashboard Widgets

**New vs Repeat Callers** (donut/pie chart)
- Two segments: new callers, repeat callers
- Mock: 65% new, 35% repeat
- Data source: `kpi_new_vs_repeat_callers`

**Call End Reasons** (donut/pie chart)
- Segments: "Customer ended", "Assistant ended", "Other"
- Mock: 70% customer-ended, 25% assistant-ended, 5% other
- Data source: `kpi_call_end_reasons`

**Top Callers** (small table, top 5)
- Columns: Phone Number, Total Calls, Total Minutes
- Mock: 5 rows of phone numbers with 3-8 calls each
- Data source: `kpi_top_callers`

### 4.6 Recent Activity Feed
A simple list below the charts showing the last 10 events:
- "📅 Appointment booked: [title] on [date] at [time]"
- "🗑️ Appointment cancelled: [title]"
- "📞 Call from [number] — [duration]"
- Mock: 10 realistic entries with timestamps
- Data source: merged from `booked_events` and `call_logs`, sorted by timestamp

---

## 5. Page: Call History

A searchable, filterable table of all calls handled by the organization's assistants.

### 5.1 Filters Bar (top of page)
- Date range picker (start date — end date)
- Assistant dropdown (if org has multiple assistants)
- Search by caller phone number (text input)
- End reason filter dropdown

### 5.2 Call Log Table
Sortable columns. Paginated (25 rows per page).

| Column | Mock Data | Data Source (Phase 2) |
|--------|----------|----------------------|
| Date/Time | "Mar 8, 2026 2:34 PM" | `call_logs.call_started_at` |
| Caller | "+1 (989) 295-5900" | `call_logs.caller_number` |
| Duration | "1m 24s" | `call_logs.duration_seconds` |
| Tools Used | "get_availability, book_appointment" | `call_logs.tools_called` |
| End Reason | "customer-ended-call" | `call_logs.end_reason` |
| Cost | "$0.03" | `call_logs.cost` |

### 5.3 Call Detail View (expandable row or slide-out panel)
When clicking a row, show:

- **Summary** — AI-generated summary (mock: 2-3 sentence summary of a booking call). Shows "Summary not available" if null.
- **Transcript** — Full call transcript (mock: 10-15 lines of back-and-forth dialogue). Shows "Transcripts are not enabled for this assistant. Contact support to enable." if null.
- **Recording** — Audio player with play/pause button (mock: disabled player with "Recording not available" if no URL). Shows audio player if URL present.
- **Tools Called** — Detailed list with timestamps (mock: 2-3 tool calls)
- **Call Metadata** — Call ID, Assistant ID, Inbound Number, Start Time, End Time

### 5.4 Call Analytics Sub-section (below or as a tab)
Summary stats for the filtered date range:

| Stat | Data Source |
|------|------------|
| Total calls in range | Count from filtered results |
| Total minutes | `kpi_call_minutes_by_org_daily` summed |
| Average duration | Computed from filtered results |
| Total cost in range | Sum of `call_logs.cost` |
| Short/abandoned calls (< 10s) | `kpi_short_abandoned_calls` |

---

## 6. Page: Calendars & Scheduling

This page manages everything related to the organization's calendars.

### 6.1 Calendar Selector
If the org has multiple calendars, show tabs or a dropdown to switch between them.
- Display: Calendar email, provider (Google), connection status indicator (green dot if active)
- Mock: 2 calendars — "hubertjanus99@gmail.com (Active)" and "mysticmango49@gmail.com (Active)"
- Data source: `calendar_config` filtered by `org_id`

### 6.2 Weekly Schedule Editor
A visual weekly grid editor for setting regular business hours.

**Layout:**
- 7 rows (Monday–Sunday), each showing the day name
- Each row shows current shifts as colored time blocks (e.g., "12:00 PM - 8:00 PM")
- "Add Shift" button on each row to add additional shifts (supports multiple shifts per day)
- "Remove" button on each shift
- "Mark as Closed" toggle per day (removes all shifts for that day)
- "Save Schedule" button at the bottom

**Mock data:**
- Mon–Fri: 12:00 PM - 8:00 PM (one shift each)
- Sat: 9:00 AM - 5:00 PM
- Sun: Closed

**Time inputs:** Use time pickers or dropdown selects in 15-minute increments (9:00, 9:15, 9:30, etc.)

**Data source:** `calendar_availability_hours` (day_of_week, open_time, close_time, calendar_id)
**Write behavior:** DELETE all rows for this calendar_id, then INSERT the new schedule.

### 6.3 Schedule Overrides
A section below the weekly schedule for managing date-specific overrides.

**Upcoming Overrides Table:**
| Column | Description |
|--------|------------|
| Date | Override date |
| Status | "Closed" (red badge) or "Custom Hours: 9:00 AM - 12:00 PM" (yellow badge) |
| Reason | e.g., "Christmas", "Half day", "Sick day" |
| Active | Toggle switch |
| Actions | Edit, Delete (soft delete via is_active = FALSE) |

**Add Override Button → Modal/Form:**
- Date picker
- Toggle: "Closed all day" vs "Custom hours"
- If custom hours: shift inputs (open_time, close_time) with "Add Shift" for multiple shifts
- Reason (text input, optional)
- Save button

**Mock data:**
- Dec 25, 2026 — Closed — "Christmas"
- Dec 24, 2026 — Custom: 9:00 AM - 12:00 PM — "Christmas Eve (half day)"
- Jan 1, 2027 — Closed — "New Year's Day"

**Data source:** `calendar_availability_overrides`

### 6.4 Calendar Settings
A form section with labeled inputs for each editable parameter.
Each input should have: a clear label, a help text description, and appropriate validation.

| Parameter | Label | Input Type | Min | Max | Help Text | Mock Value |
|-----------|-------|-----------|-----|-----|-----------|------------|
| `front_overhang_mins` | Buffer Before Appointments | Number | 5 | 60 | "Minutes of buffer time before each appointment. Prevents back-to-back bookings." | 15 |
| `back_overhang_mins` | Buffer After Appointments | Number | 0 | 60 | "Minutes of buffer time after each appointment." | 0 |
| `min_event_duration_mins` | Minimum Appointment Length | Number | 5 | (max_event) | "Shortest appointment your callers can book (minutes)." | 15 |
| `max_event_duration_mins` | Maximum Appointment Length | Number | (min_event) | 480 | "Longest appointment your callers can book (minutes)." | 60 |
| `default_event_duration_mins` | Default Appointment Length | Number | (min) | (max) | "Default appointment length when a caller doesn't specify a duration." | 30 |
| `max_bookings_per_caller` | Max Bookings Per Caller | Number | 1 | 20 | "Maximum number of upcoming appointments one caller can have at a time." | 3 |
| `min_booking_lead_time_hours` | Minimum Booking Notice | Number | 0 | 72 | "How many hours in advance appointments must be booked." | 1 |
| `max_booking_lead_time_days` | Maximum Booking Window | Number | 1 | 90 | "How far into the future callers can book (days)." | 30 |
| `min_cancellation_lead_time_hours` | Minimum Cancellation Notice | Number | 0 | 72 | "How many hours in advance appointments can be cancelled or rescheduled." | 0 |
| `event_metadata_visible` | Show Caller Details in Calendar | Toggle | — | — | "When enabled, caller name, phone, and email appear in Google Calendar event descriptions." | ON |
| `respect_transparent_events` | Block 'Free' Calendar Events | Toggle | — | — | "When enabled, events marked as 'free' on your Google Calendar will still block those time slots from being booked." | ON |
| `event_match_window_mins` | Appointment Match Window | Number | 5 | 120 | "How precisely callers need to specify appointment time when cancelling or rescheduling (minutes). Lower values require more precision." | 30 |

**"Save Changes" button** at the bottom with confirmation toast on success.

**Data source:** `calendar_config` — UPDATE individual fields.

### 6.5 Assistant Calendar Authorization
A section showing which assistants are linked to this calendar.

| Column | Description |
|--------|------------|
| Assistant | Assistant ID (or friendly name if available) |
| Calendar | Calendar email |
| Authorized | Toggle switch (green = authorized, gray = not authorized) |

**Mock data:**
- Assistant "367f9dd0-..." → hubertjanus99@gmail.com → Authorized ✅
- Assistant "367f9dd0-..." → mysticmango49@gmail.com → Authorized ✅

**Data source:** `assistant_calendar_link`
**Write behavior:** UPDATE `is_authorized` for the specific assistant-calendar pair.

**Warning:** Show a confirmation dialog when deauthorizing: "Deauthorizing this calendar will prevent the assistant from booking, cancelling, or rescheduling appointments on this calendar. Are you sure?"

### 6.6 Booking Analytics (sub-section or tab on Calendars page)

**Bookings by Appointment Type** (bar chart or table)
- Data source: `kpi_bookings_by_appointment_type`
- Mock: "Electrical: 12", "Plumbing: 8", "General: 23"

**Popular Booking Days** (bar chart)
- Data source: `kpi_popular_booking_days`
- Mock: bars for Mon–Sun

**Popular Booking Times** (bar chart)
- Data source: `kpi_popular_booking_times`
- Mock: bars for each hour of day

**After Hours Bookings** (stat card)
- Data source: `kpi_after_hours_bookings`
- Mock: "12 bookings (28%) were made after business hours"

---

## 7. Page: Settings

### 7.1 Assistant Management
Show a card or section for each assistant in the org.

**Assistant Info (read-only):**
- Assistant ID (truncated with copy button)
- Inbound Phone Number (formatted)
- Status: Active indicator (green dot) — read-only, not a toggle
- Call Logging: "Enabled" or "Disabled" with note "(Contact support to change)"
- Transcripts: "Enabled" or "Disabled" with note "(Contact support to change)"
- Summaries: "Enabled" or "Disabled" with note "(Contact support to change)"

**Block List Editor:**
- Current blocked numbers displayed as removable chips/tags (e.g., "+1 (555) 123-4567 ✕")
- "Add Number" section with three input fields side by side:
  - Country Code: text input, prefilled with "+1", max 4 chars
  - Area Code: text input, placeholder "555", exactly 3 digits
  - Number: text input, placeholder "1234567", exactly 7 digits
- "Add to Block List" button
- Validation: all three fields required, digits only, proper length. Show inline error if invalid.
- Assembled format: "+{country}{area}{number}" (e.g., "+15551234567")
- Remove button (✕) on each chip with confirmation: "Remove +1 (555) 123-4567 from the block list?"

**Mock data:**
- 2 blocked numbers: "+19895551234", "+12125559876"

**Data source:** `assistant_config.block_list` (JSON array)
**Write behavior:** UPDATE `assistant_config` SET block_list = [updated_array]

### 7.2 Notification Settings
A table showing all notification rules for the org.

| Column | Description |
|--------|------------|
| Event | Friendly name: "New Booking", "Cancellation", "Reschedule", "Availability Check" |
| Channel | "Pushover" or "Slack" with icon — read-only |
| Recipient | Label from notification_config (e.g., "Gavin", "Team") — read-only |
| Enabled | Toggle switch |

**Mock data:** 8 rows matching the notification_config screenshot (4 Pushover + 4 Slack rules, various enabled states)

**Data source:** `notification_config`
**Write behavior:** UPDATE `notification_config` SET is_enabled = [value] WHERE id = [row_id]

**Note displayed below table:** "To set up new notification channels or change recipients, contact support."

### 7.3 Account Settings
- **Email** — read-only display of user's email
- **Organization** — read-only display of org name
- **Change Password** — button that triggers Supabase Auth password reset flow
- **Sign Out** — button

---

## 8. KPI View Reference

Complete list of all 30 Supabase KPI views available for data display. All views have `org_id` for filtering.

### Used in Dashboard Page
| View | Used For |
|------|---------|
| `kpi_org_monthly_summary` | Primary KPI cards (calls, bookings, cost, duration, unique callers) |
| `kpi_booking_conversion_rate` | Conversion rate card |
| `kpi_cancellation_rate` | Cancellation rate card |
| `kpi_cost_per_booking` | Cost per booking card |
| `kpi_call_minutes_by_org_daily` | Call volume over time chart |
| `kpi_bookings_by_calendar_daily` | Bookings over time chart |
| `kpi_calls_by_hour` | Calls by hour chart |
| `kpi_calls_by_day_of_week` | Calls by day of week chart |
| `kpi_new_vs_repeat_callers` | New vs repeat callers donut chart |
| `kpi_call_end_reasons` | Call end reasons donut chart |
| `kpi_top_callers` | Top callers table |

### Used in Call History Page
| View | Used For |
|------|---------|
| `kpi_short_abandoned_calls` | Short/abandoned call stats |
| `kpi_call_minutes_by_org_daily` | Call analytics summary |

### Used in Calendars Page
| View | Used For |
|------|---------|
| `kpi_bookings_by_appointment_type` | Bookings by type chart |
| `kpi_popular_booking_days` | Popular booking days chart |
| `kpi_popular_booking_times` | Popular booking times chart |
| `kpi_after_hours_bookings` | After hours booking stat |

### Available for Future Use (not in v1 but wired to views)
| View | Potential Use |
|------|--------------|
| `kpi_avg_cost_per_min_by_org` | Detailed cost analysis page |
| `kpi_bookings_by_assistant_daily/monthly/yearly` | Per-assistant booking trends |
| `kpi_bookings_by_calendar_monthly/yearly` | Long-term booking trends |
| `kpi_call_minutes_by_assistant_daily/monthly/yearly` | Per-assistant usage breakdown |
| `kpi_call_minutes_by_org_monthly/yearly` | Long-term usage trends |
| `kpi_call_tool_engagement` | Tool engagement analysis |
| `kpi_call_volume_weekly` | Weekly trend view |
| `kpi_cost_breakdown_by_org` | LLM/STT/TTS/Vapi cost breakdown |
| `kpi_latency_by_assistant` | Per-assistant performance |
| `kpi_latency_by_org` | Overall system performance |
| `kpi_latency_trend_weekly` | Performance trend monitoring |
| `kpi_tool_usage_frequency` | Which tools are used most |
| `kpi_tools_per_call` | Tool usage density |
| `kpi_unique_callers_monthly` | Caller growth tracking |

---

## 9. Non-Editable Parameters (Admin Only)

These exist in the database but are NOT exposed in the customer dashboard. Changes require contacting support.

| Table | Field | Reason |
|-------|-------|--------|
| `assistant_config` | `is_active` | Turning off stops all call routing — too dangerous for self-service |
| `assistant_config` | `call_logging_enabled` | Requires prompt changes for legal compliance (wiretapping laws) |
| `assistant_config` | `transcripts_enabled` | Requires prompt changes for legal compliance (wiretapping laws) |
| `assistant_config` | `summary_enabled` | Tied to transcript toggle |
| `assistant_config` | `assistant_id` | Core system identifier |
| `assistant_config` | `inbound_number` | Vapi phone number config |
| `calendar_config` | `is_active` | Deactivating breaks all tools with error 805 — too dangerous |
| `calendar_config` | `refresh_token` | OAuth credential — security sensitive |
| `calendar_config` | `auth_method` | System config |
| `calendar_config` | `calendar_provider` | System config |
| `calendar_config` | `google_sync_token` | Sync infrastructure |
| `calendar_config` | `watch_*` fields | Push notification infrastructure |
| `notification_config` | `channel` | Requires admin setup |
| `notification_config` | `channel_config` | Contains API keys — security sensitive |
| `assistant_calendar_link` | `verification_mode` | Security setting |
| `organization_config` | Everything | Core org identity and auth credentials |

---

## 10. Design Guidelines

### General
- Clean, professional SaaS dashboard aesthetic
- Dark sidebar with light content area (standard dashboard pattern)
- Mobile responsive — sidebar collapses to hamburger on small screens
- Use Recharts for all charts (React-native, works with Tailwind)

### Components
- KPI cards: number + label + optional trend indicator (↑ or ↓ with percentage)
- Data tables: sortable columns, pagination, row hover state
- Forms: clear labels above inputs, help text below, inline validation errors
- Toggles: shadcn/ui Switch component
- Time pickers: dropdown selects in 15-minute increments
- Date pickers: shadcn/ui Calendar/DatePicker component

### Feedback & States
- Loading skeletons for all data-fetching areas (not spinners)
- Empty states with helpful messaging ("No calls yet — calls will appear here once your receptionist handles its first call")
- Toast notifications for successful saves (green) and errors (red)
- Confirmation dialogs for destructive actions (removing blocked number, deauthorizing calendar)
- Disabled/grayed state for read-only fields with "Contact support" notes

### Mock Data Quality
- Use realistic phone numbers (formatted as +1 (XXX) XXX-XXXX)
- Use realistic appointment titles ("Outlet Repair", "Annual Checkup", "Consultation")
- Use realistic cost values ($0.01 - $0.05 per call range)
- Use realistic durations (30s - 5min range for calls)
- Chart data should show realistic patterns (weekday > weekend, business hours peak)

---

## 11. Implementation Order

### Phase 1: Gemini (Visual Scaffold)
1. Project scaffolding — Vite + React + Tailwind + shadcn/ui + React Router
2. Auth pages — Login, signup, password reset, "Account Pending" placeholder (all visual only, no real auth)
3. Layout shell — Sidebar navigation, header with user menu, main content area
4. Dashboard page — All KPI cards + all charts with mock data
5. Call History page — Filterable table + expandable detail view with mock data
6. Calendars page — Schedule editor, overrides section, calendar settings form, authorization toggles
7. Settings page — Block list editor, notification toggles table, account settings

### Phase 2: Backend Wiring (Claude)
1. Supabase client setup + environment variables
2. Auth wiring — Supabase Auth for login/signup/logout, protected routes, org lookup
3. RLS policies — Lock down all tables for frontend access via authenticated user + org membership
4. Dashboard data — Replace mock data with real Supabase queries to KPI views
5. Call History data — Real queries to call_logs with filtering/pagination
6. Calendar management — Real CRUD for availability hours, overrides, calendar settings
7. Settings — Real CRUD for block list, notification toggles
8. Testing — End-to-end with real data
